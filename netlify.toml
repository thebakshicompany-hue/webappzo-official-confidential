[build]
  command = "npm run build"
  publish = ".next"
  base = "webappzodash/frontend"
  environment = { NODE_VERSION = "18", NPM_FLAGS = "--legacy-peer-deps", SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY" }